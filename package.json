{
  "name": "dccwifi-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "lucide": "^0.475.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.44.0",
    "@tailwindcss/vite": "^4.0.0-beta.0",
    "tailwindcss": "^4.0.0-beta.0",
    "vite": "^6.0.0",
    "wrangler": "^4.110.0"
  }
}
